#include <stdio.h>
#include "main.h"

/**
 * main - prints all alphabet in lowercase.
 * Return: (0) success.
 */

void main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)

	{
		_putchar(i);
	}
	_putchar('\n');

	return (0);

}
