#include <stdio.h>
#include "main.h"

/**
 * main - prints 10 times the alphabet, in lowercase.
 * Return: always 0 (success)
 */

void main(void)
{
	int ten;
	char i;

	for (i = 'a'; i <= 'z', i++)

	{
		for (ten = '0'; ten <= '10'; ten *)
			_putchar(i);
		_putchar('\n');

		return (0);
}
